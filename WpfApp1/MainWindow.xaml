<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid VerticalAlignment="Center">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="Login" Style="{StaticResource HeaderTextBlockStyle}" />

                <TextBox PlaceholderText="Username" Style="{StaticResource InputTextBoxStyle}" />
                <PasswordBox PlaceholderText="Password" Style="{StaticResource InputPasswordBoxStyle}" />

                <Button Content="Login" Style="{StaticResource LoginButtonStyle}" />
            </StackPanel>
        </Grid>
    </Grid>

</Window>
